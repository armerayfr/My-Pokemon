const { Router } = require("express");
const router = Router();

const {} = require("../controllers/pokemon");

router.post("/pokemon");
router.post("/pokemon");
router.post("/pokemon");

module.exports = router;
